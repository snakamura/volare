$newline never

$if not (null flights)
  <ol>
    $forall Entity flightId flight <- flights
      <li>
        <a href=@{FlightR flightId}>#{flightName flight}

<form method=POST action=@{FlightsR}>
  ^{flightWidget}
  <input type=submit>
